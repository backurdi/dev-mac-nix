{
  # Change these values before applying on a Mac with a different account.
  username = "bachir.kurdi";
  fullName = "Bachir Kurdi";
  email = "bachir.kurdi@gmail.com";

  # This configuration targets Apple Silicon.
  system = "aarch64-darwin";

  # Xcode is installed through the Mac App Store. The target Mac must be
  # signed in to the App Store before the first activation.
  installXcode = true;
}
